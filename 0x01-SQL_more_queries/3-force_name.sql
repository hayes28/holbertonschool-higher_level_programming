-- Create table 'force_name' with descriptions
-- Had to change into 'hbtn_0d_2' database to use
-- command in server. 'USE hbtn_0d_2;'
CREATE TABLE IF NOT EXISTS force_name (
    id INT,
    name VARCHAR(256) NOT NULL
);
